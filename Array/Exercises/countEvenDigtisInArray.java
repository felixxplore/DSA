package Array.Exercises;

public class countEvenDigtisInArray {

  public static void main(String[] args) {
    // int[] a = { 12, 345, 2, 6, 7896 };
    // int count = 0;
    // for (int i = 0; i < a.length; i++) {
    //   String value = Integer.toString(a[i]);

    //   if (value.length() % 2 == 0) {
    //     count++;
    //   }
    // }

    // System.out.println(count);

    int num = -123;
    if (num < 0) {
      num *= -1;
    }
    System.out.println((int) (Math.log10(num)) + 1);
  }
}
